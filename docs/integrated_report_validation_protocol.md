# Integrated report transfer protocol

Freeze the complete current reporting workflow and this driver before running
PowerModels case6 and case7_tplgy through four predetermined source-backed probes.
A preselection filename search found no references in project tests, benchmarks,
or docs outside JSON. Fixture headers were inspected without running diagnostics.
Case6 explicitly duplicates the case3 topology, so it is not an independent network
sample; case7_tplgy probes component-status preprocessing. These are public software
test fixtures, not blind or real incident evidence. No replacement follows failure.

For both fixtures, preserve the raw source as a hash-pinned reference and evaluate:

| Probe | Predetermined criterion |
| --- | --- |
| Clean | Integrated report returns an accepted verified primal point. |
| Reactive-load units | Divide per-unit qd by baseMVA again; report a source mismatch affecting qd. |
| Stale reference | Append a harmless comment to a source copy while retaining the original expected hash; explicitly report source-contract unavailability. |
| Zero capacity | Set active generator pmax to zero and lower pmin to at most zero; report an island contradiction. |

These eight criteria test full-path coverage. Early model-scope refusal remains a
failed coverage criterion even if the local refusal is correct. It must not be
misreported as a false mathematical conclusion. Failures and unavailable results
stay in the denominator. The report may stop before downstream source checks; the
stage trace must show that clearly. Input and report artifacts are saved per probe.

Hash the executable driver, all prior frozen inputs, all current workflow helpers,
these fixtures/licenses, and this protocol before execution. Verify hashes around
every case, and refuse output overwriting. Do not tune code or change criteria
within the run. Harness integrity assertions are separate from the acceptance gate.
The historical 11/14 gate remains untouched. No human repair benefit is measured.
